---
import ProjectCard from "./ProjectCard.astro";
import { projects } from "../data/projects";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import { splitWords } from "../utils/text";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---
<section class="section" id="work">
  <div class="section-inner">
    <header class="section-head">
      <h2 data-reveal>
        {splitWords(t("work.title")).map((word, i, arr) => (
          <>
            <span class="word-mask"><span class="word" style={`--i:${i}`}>{word}</span></span>
            {i < arr.length - 1 ? " " : ""}
          </>
        ))}
      </h2>
      <p class="section-lead" data-reveal>{t("work.lead")}</p>
    </header>

    <div class="project-grid">
      {projects.map((project) => <ProjectCard project={project} />)}
    </div>
  </div>
</section>

<style>
  .project-grid {
    display: grid;
    /* auto-fill, no auto-fit: con un solo proyecto, auto-fit colapsa las
       pistas vacías y estira esa única tarjeta a los ~1150px del contenedor
       -- el placeholder de gtm-factory llegó a renderizar a ~1152×720px por
       esto. auto-fill deja las pistas vacías ocupando espacio (sin colapsar),
       así que un ítem solo queda del ancho de su propia columna. */
    grid-template-columns: repeat(auto-fill, minmax(20rem, 1fr));
    gap: 1.5rem;
    margin-top: 3rem;
  }
</style>
