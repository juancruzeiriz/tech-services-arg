---
// Las demás secciones (Work, About, Certs, CV, Contact) se agregan en la
// Tarea 1.8 — por ahora Hero ya es contenido real, no placeholder.
import Base from "../../layouts/Base.astro";
import Hero from "../../components/Hero.astro";
import { languages } from "../../i18n/utils";

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({ params: { lang } }));
}
---
<Base>
  <Hero />
  <section style="padding: 4rem 2rem 6rem; min-height: 40vh;">
    <p>placeholder — Tarea 1.8 completa el resto del contenido</p>
  </section>
</Base>
